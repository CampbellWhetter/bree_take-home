{
  "name": "bree-take-home",
  "version": "1.0.0",
  "description": "Loan Application Processor — Backend (Python)",
  "private": true,
  "scripts": {
    "install": "pip install -r requirements.txt",
    "start": "python -m uvicorn src.app:app --host 0.0.0.0 --port 8000"
  }
}
